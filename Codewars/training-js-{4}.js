getLength = (arr) ->
    arr.length


getFirst = (arr) ->
    arr[0]

getLast = (arr) ->
    arr[arr.length - 1]


pushElement = (arr) ->
    el = 1
arr.push el
arr


popElement = (arr) ->
    arr.pop()
arr
